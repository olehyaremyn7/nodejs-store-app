<div class="container">
    <div class="jumbotron">
        <div class="product">
            <h1>{{ product.title }}</h1>
            <img src="{{ product.image }}" alt="{{ product.title }}">
            <p class="prod-price card-price">{{ product.price }}</p>
            <p>{{ product.description }}</p>
        </div>
    </div>
</div>